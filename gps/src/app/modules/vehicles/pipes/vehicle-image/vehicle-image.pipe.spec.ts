import { VehicleImagePipe } from './vehicle-image.pipe';

describe('VehicleImagePipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleImagePipe();
    expect(pipe).toBeTruthy();
  });
});
